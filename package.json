{
  "name": "e-date-server",
  "version": "1.0.0",
  "description": "Backend for E-Date App",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "pm2 start server.js --name e-date-server && pm2 monit",
    "kill": "pm2 stop e-date-server && pm2 delete 0"
  },
  "author": "Victor Santaella",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^2.4.0"
  }
}
